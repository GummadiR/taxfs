# Changelog (one line per merged change — Blueprint §8)

- F1: monorepo skeleton + full gate chain (lint/money-lint, values audit, typecheck, production build, unit, e2e vs production, CI) with G3/G4 negative tests and per-gate break proofs.
- F1: build gate hardened (build-strict: Error lines in next build output fail the gate even at exit 0) + per-gate break proofs recorded in GATE-PROOFS.md.
- F2: §4 schema v2 as migrations (FORCE RLS everywhere, composite PKs, roles, storage policies), G1/G2 isolation suite (14 negative tests) on a real Postgres in CI, Supabase SSR auth in the web app, BYPASSRLS-definer design decision recorded.
- F3: core ported verbatim from TaxOS (shared/Money/rules, kernel, kernel2, 42 goldens, divergence suite; tests renamed by subject). TaxFS walls caught real defects in the port: money-lint found 3 native '+' in kernel2 (outside TaxOS's narrower scope), and the G4 audit found the §219(g)(2)(B) $200 floor hardcoded in BOTH kernels — now a cited rule-data figure. Audit scanner rewritten (char-level, money-literal pass).
- F3: spine (contracts + in-memory reference; PgSpine deliberately not ported) and gates ported; ACC-TIEOUT-FORM rewritten graph-derived per §3.2 — kernel form-total lines emit signed `terms`, the critic re-adds exactly those (P76/P94 drift class structurally gone). 547 tests green.
- F3: forms ported (mapping, XML, packages, PDF fill, field-map coverage guard) + official 2025 templates; G10 negative test added (seeded bogus field name caught against the real f1040.pdf). Phase 3 complete: 700 tests green.
- F4: Spine v2 — PgSpine on the §4 schema (workspace-scoped statements, composite-PK conflicts, findings-as-payload, terms persisted, RLS-bypass connections refused at connect) + migration 0003 (rich registers, gate-run staleness columns, run-id sequence, id-carrying audit triggers). The shared contract suite passes 16/16 against it; 716 tests green.
- F4: the app — agents ported; stateless server layer (per-request spine, no global session cache), Get Started/Documents/Review-with-drilldown/Gates Board/File It pages; local-operator mode (same RLS walls, refuses to coexist with hosted auth); e2e journey 9/9 against the production build over the real migrations. 779 tests green.
