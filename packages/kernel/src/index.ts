/**
 * @taxfs/kernel — Phase 1 placeholder.
 * The computation kernel is ported verbatim from TaxOS in Phase 3
 * (Blueprint §2, §7.3). This file exists so the money-safety lint scope
 * (eslint.config.mjs) binds from day one: any code that lands here later
 * is already inside the wall.
 */
export const KERNEL_PORTED = false;
