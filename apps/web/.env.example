# Supabase (Phase 2). Copy to .env.local (gitignored). The anon key is safe
# for the browser; RLS is the wall. The service-role key is deliberately NOT
# listed: it must never appear on request paths (Blueprint §4 (f)).
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key
